import React from "react";
import "./Header.css";
// import ProgressBar from "react-bootstrap/ProgressBar";

const Header = () => {
  return (
    <header className="header">
      <nav>
        <h1>⚕️ Sacred Heart</h1>
      </nav>
      {/* <div>
        <ProgressBar now={progress} />
      </div> */}
    </header>
  );
};

export default Header;
